# 功能实施确认书：K-means 抠图降级 + Canvas 导出引擎

> 评审角色：PM × 架构师 × UI/UX 设计师
> 基于自有设计风格（CoverForge Pro 暗夜科技风）确认

---

## Feature A：K-means 抠图降级

### PM 确认

| 维度 | 结论 |
|------|------|
| **为什么做** | ONNX 抠图 CDN 内容变更（返回 PyTorch 而非 ONNX），功能完全不可用。K-means 是唯一零依赖、立即可用的降级方案 |
| **用户价值** | 从「功能不可用」→「基本可用」。效果不如 ONNX（复杂背景/精细边缘差一些），但能抠人像/产品主体 |
| **体验降级策略** | 首次使用时 toast 提示「当前使用基础抠图模式；安装深度学习模型后可获得更精准效果」 |
| **未来升级路径** | ONNX 模型修好后自动切换，用户无感知 |

### 架构师确认

| 维度 | 结论 |
|------|------|
| **技术方案** | V2.html 第 1037-1141 行的 `kmeansCutout()`，基于 Canvas2D ImageData 操作 |
| **算法流程** | 边缘采样（四边 + 四角）→ k-means++ 初始化（k=3）→ 迭代聚类（max 12 轮）→ 距离判定（t0/t1 双阈值）→ alpha 羽化 → 边缘平滑（3×3 高斯）|
| **性能** | 800×800 图片 ≈ 150-400ms（实测值）|
| **内存** | 仅操作 ImageData，无额外 blob/file 开销 |
| **兼容性** | 所有现代浏览器（Canvas2D 标准 API）|
| **依赖** | 零外部依赖 |
| **集成方式** | 放入 `utils/cutout.ts`，ONNX 抛出特定错误时自动 fallback |

### UI/UX 设计师确认

| 层面 | 设计 |
|------|------|
| **触发** | 与当前「一键抠图」按钮逻辑一致，点击后进入 loading 态 |
| **进度提示** | 使用现有 Toast 系统：`🖼️ 抠图中（K-means 聚类）...` → `✅ 抠图完成（280ms）` |
| **降级提示** | 首次触发 K-means 时：紫色 info toast「当前为基本抠图模式，复杂背景效果有限」、5 秒自动消失 |
| **状态重置** | 点击「撤消」可回退到原图 |
| **异常处理** | 图片过小（<50px）→ toast「图片太小无法抠图」；图片过大（>2000px）→ 自动缩放到 1000px 后处理 |

---

## Feature B：Canvas 渐变导出引擎

### PM 确认

| 维度 | 结论 |
|------|------|
| **为什么做** | 模板背景用了复杂 CSS 渐变（`linear-gradient` + `radial-gradient` 多层叠加），html2canvas 导出时有时出现偏色/错位/闪烁 |
| **用户价值** | 导出 = 所见即所得。背景色/渐变/滤镜/阴影 100% 还原浏览器预览效果 |
| **功能范围** | 仅替换背景渲染层，元素渲染仍保留 html2canvas（文字/图片/形状的渲染更准确）|
| **可选方案** | 提供「基础导出（html2canvas）」和「增强导出（Canvas2D）」两个按钮，用户可自行选择 |

### 架构师确认

| 维度 | 结论 |
|------|------|
| **核心代码** | V2.html 8 个函数（~200 行）：
| | - `drawBackgroundLayers` — 多层背景绘制入口 |
| | - `paintBgLayer` — 单层背景（纯色 / linear-gradient / radial-gradient）|
| | - `splitTopLevel` — 顶层逗号分割（处理括号嵌套）|
| | - `parseLinearAngle` — CSS 角度解析（deg / to 方向）|
| | - `linearGradientLine` — 渐变线端点计算 |
| | - `parseColorStop` / `normalizeStops` — 颜色断点解析 + 插值填充 |
| | - `parseRadialSize` / `parsePosition` — 径向渐变参数解析 |
| **导出流程** | out Canvas（2x scale）→ drawBackgroundLayers → html2canvas 元素层叠加 → 元素层图像合成 |
| **混合模式** | Canvas2D 画背景 → html2canvas 绘制元素 → `ctx.drawImage()` 合成到输出 canvas |
| **兼容性** | `ctx.filter` 在 Safari 15+ 支持；不支持时降级为无滤镜导出并 toast 提示 |
| **文件位置** | 新建 `utils/exportBgEngine.ts`（~250 行）|
| **测试要点** | 6 个模板的渐变背景导出对比 + 自定义渐变色 + 纯色背景 + 透明背景 |

### UI/UX 设计师确认

| 层面 | 设计 |
|------|------|
| **触发** | 导出按钮保持现有蓝色样式，点击后弹出导出选项弹窗 |
| **导出弹窗** | 保持现有简洁风格，增加「导出模式」切换 |
| | `💎 增强模式（推荐）` — 渐变/滤镜 100% 还原 |
| | `📦 标准模式` — 使用 html2canvas（元素更精确）|
| **下载** | 两种模式都输出 2x PNG，文件名区分 `xxx_enhanced.png` / `xxx_standard.png` |
| **进度** | 增强模式可能多 200-500ms，显示「导出中...（增强背景引擎）」|

---

## 实施路线（合并版）

### 文件改动清单（≤3 / 批）

| 批次 | 文件 | 改动 |
|------|------|------|
| 1 | `client/src/utils/cutout.ts` | 新增 `kmeansCutout()` + fallback 逻辑 |
| 1 | `client/src/utils/exportBgEngine.ts` | 新增（移植 V2 渐变渲染 8 函数 + 导出引擎）|
| 2 | `client/src/components/Toolbar.tsx` | 导出弹窗支持模式切换 + 调用增强引擎 |

### 验证标准

| 场景 | 预期 |
|------|------|
| K-means 抠图（800×600 人像）| 主体保留，背景移除，耗时 <500ms |
| K-means 抠图（复杂背景）| 边缘有残留，但主体可辨 |
| ONNX 衰退 → K-means 自动降级 | 首 toast 提示降级 |
| HTML 渐变背景导出（增强模式）| 与浏览器预览一致 |
| 渐变导出 vs html2canvas 对比 | 增强模式更准确 |
| 所有模板导出 | 颜色/阴影/滤镜 100% 还原 |

---

## 评审结论：✅ 确认实施

三个角色均无红线问题，同意按上述方案实施。
