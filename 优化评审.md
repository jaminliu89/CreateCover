# CreateCover 优化评审报告

> 产品经理 + 架构师双视角，2026-07-25

---

## 一、架构层面（技术债务）

### 1.1 严重 — 零测试覆盖

整个项目 987 个文件，**0 个测试文件**。Core 模块（画布交互/元素变换/对齐算法/历史栈）全凭手工验证。

**建议**：
- P0：给核心算法（对齐/分布/构图/配色）加单元测试 → `src/utils/*.test.ts`
- P1：给画布拖拽/缩放/旋转加 Playwright E2E 测试
- P2：给 Store actions（增删改元素/历史/编组）加 integration test

### 1.2 严重 — TypeScript strict 未开启

`tsconfig.app.json` 中 **未开启 `strict: true`**，导致：
- `strictNullChecks` 缺失 → 潜在空引用崩溃（`selectedId` 为 null 时访问其属性）
- `noImplicitAny` 缺失 → 15+ 处 `as any` 类型绕过（Canvas.tsx 中 15 处，store 中 20+ 处）
- `noImplicitReturns` 缺失 → 函数可能隐含返回 undefined

**建议**：分两步开启：
1. 先开 `strictNullChecks` + `noImplicitAny`，修完所有报错
2. 再开完整 `strict: true`

### 1.3 中 — Store 单文件 682 行，职责混杂

`useEditorStore.ts` 集成了：
- 状态定义（80 行 interface）
- 增删改元素（~60 行）
- 对齐/分布算法（~80 行 — 应抽到 `utils/alignment.ts`）
- 编组/解组（~60 行）
- 栅格化（~50 行 — 应抽到 `utils/rasterize.ts`）
- 图层管理（~70 行 — 可抽到 `utils/layer.ts`）
- 撤销/重做（~50 行）
- 持久化配置（~35 行）

**建议**：
```
store/
  useEditorStore.ts      → 核心状态 + 基础 actions（~250 行）
  useHistoryStore.ts     → 撤销/重做
  useProjectStore.ts     → 项目 CRUD + 预设
utils/
  alignment.ts           → 对齐/分布算法（从 store 迁出）
  rasterize.ts           → 已迁出 ✅
```

### 1.4 中 — Canvas.tsx 1041 行，重复代码

问题：
1. **三大 render 函数**（renderTextElement/renderImageElement/renderShapeElement）的外层 wrapper div 结构几乎完全一致（left/top/transform/stopPropagation/onMouseDown/onContextMenu）
2. **锁定图标**在 3 个 render 函数中各重复一次
3. **15+ 个独立 Zustand selector** 零散在各处
4. **魔法数字**散布（`* 3` 灵敏度 / `0.3`, `5` 缩放钳位 / `8`, `500` 字号范围）

**建议**（分 3 批，每批 ≤3 文件）：

| 批 | 改动 | 减行 |
|----|------|------|
| 1 | 提取 `ElementWrapper` 公共组件 | -180 |
| 2 | 提取 `LockBadge` 公共组件 | -30 |
| 3 | 用 `useCanvasStore` hook 聚合 selector | -50 |

### 1.5 低 — 其他

| 问题 | 位置 | 建议 |
|------|------|------|
| TextElement 缺少 `width` 字段 | `data/index.ts` | 补全类型定义，移除 `as any` |
| server 缺 error handler | `server/src/index.ts:35` | 加 Express 错误处理中间件 |
| 无 lint/typecheck 脚本 | `package.json` | 加 `"typecheck": "cd client && tsc --noEmit"` |
| 无 TODO/FIXME 注释 | 全项目 | 建议团队用 `// TODO:` 标记技术债务 |
| 引用路径依赖 | `useEditorStore.ts` 导出 `isBackgroundElement` 被 Canvas 直接 import | 抽到独立 `utils/element.ts` |

---

## 二、产品层面

### 2.1 高优先级 — 修复抠图功能

当前状态：`@imgly/background-removal` 的 CDN 内容已变更（返回 PyTorch 而非 ONNX），本地模型格式不匹配。**这是用户最可能失败的首次体验之一。**

**方案选型**：

| 方案 | 优点 | 缺点 | 工作量 |
|------|------|------|--------|
| A. 降级到 K-means 算法（已有） | 零成本，已可用 | 效果差（用户吐槽"把人抠没了"）| 0 |
| B. 升级 @imgly 到支持新 CDN 的版本 | 保持 ONNX 推理 | 需等官方更新 CDN；不确定是否有新版 | 1d |
| C. 换用 rembg（ONNX，自建 CDN）| 可控 | 需自建模型存储（Cloudflare R2 + Workers）| 2d |
| D. 降级 + 前端提示"抠图效果有限，建议使用专业工具" | 最低成本 | 用户体验降级 | 0.5d |

**推荐**：方案 D（短期）+ 方案 C（中长期）。短期先修体验降级，中长期自建模型通道。

### 2.2 高优先级 — 新用户首次体验

当前：打开应用即见模板库和空白画布，但**缺少引导**。用户可能：
- 不知道可以双击编辑文字
- 不知道模板可以点击套用
- 不知道 AI 工具箱能做什么
- 看到白画布不知所措

**建议**：
1. **首次加载弹窗**（3 步引导）：选择比例 → 套用模板 → 编辑文字 + 导出
2. **空状态提示**：画布为空时显示"点击左侧模板开始设计 / 或从空白创建"
3. **功能标注**：AI 工具箱按钮加 tooltip 说明

### 2.3 中优先级 — 模板库扩充

当前：12 个精选模板。对标 Canva/稿定设计 → 至少需 200+ 模板。

**方案**：
| 方案 | 成本 | 效果 |
|------|------|------|
| 手动制作模板上传 | 1 个模板 ≈ 15min | 需要设计人力 |
| 用户生成模板（UGC）| 开发功能，用户上传 | 需要"另存为公共模板"功能 |
| AI 自动生成模板 | 接入 LLM + Canvas 渲染 | 质量不可控 |

**推荐**：先做"统一模板格式 + 导入导出"，鼓励用户/设计师批量制作。

### 2.4 中优先级 — 数据持久化与云同步

当前：编辑状态存 localStorage；项目管理走服务端 JSON。**无法多端同步**。

**建议**（分阶段）：
| 阶段 | 功能 | 方案 |
|------|------|------|
| P1 | 自动保存到云端 | 每 30s + 切页面时自动 sync |
| P2 | 多端同步 | 版本冲突检测 + 最后写入胜出 |
| P3 | 历史版本 | 保存每次发布快照，支持回滚 |

### 2.5 中优先级 — 移动端适配

当前：仅桌面端可用（画布 405-720px，布局依赖宽屏）。**大量自媒体用户使用手机**。

**建议**：
- Stage 1：响应式布局，768px 以上可编辑
- Stage 2：768px 以下显示"请使用电脑编辑"提示 + 二维码打开
- Stage 3：手机端简化版（快速换文字 + 导出）

### 2.6 低优先级 — 功能增强

| 功能 | 理由 | 建议方案 |
|------|------|----------|
| AI 标题生成（真·LLM）| 当前是预置库，不自由 | 接入硅基流动 API（用户自配 Key）|
| 图片搜索 | 用户需要找素材 | 接入 Unsplash/Pexels API |
| 社交媒体一键发布 | 封面做完就发 | 微信/抖音/小红书 API |
| 团队协作 | 多成员同时编辑 | WebSocket + CRDT/rebase |
| 数据统计 | 哪些模板/功能最多人用 | 简单埋点 + 后台看板 |
| 导出格式扩充 | 当前仅 PNG | 加 JPG/WebP/PDF/SVG |

---

## 三、数据模型优化

### 3.1 修复 TextElement 类型

```typescript
// 当前：TextElement 没有 width
// 建议补全（解决 15+ 处 as any）
export interface TextElement {
  // ... 现有字段
  width?: number           // 新增，文字框百分比宽度（可选，不设则自适应内容）
  maxWidth?: number        // 新增，最大宽度约束
}
```

### 3.2 存贮迁移路线

| 阶段 | 存储后端 | 容量 | 离线 |
|------|----------|------|------|
| 现在 | localStorage | 5MB | ✅ |
| P1 | IndexedDB + 云端 | 无限制 | ✅ |
| P2 | Cloudflare D1 + R2 | TB 级 | ⚡待缓存 |
| P3 | CRDT 多端同步 | 无限制 | ✅ |

---

## 四、优化路线图（按优先级排序）

```
P0 (本周)
├── 🐛 修复抠图不可用（降级提示 + 修 publicPath）
├── 🧪 核心算法加测试（对齐/布局/配色）
└── 🛡️ 开启 strictNullChecks

P1 (本月)
├── 🏗️ Store 拆分（history/project 子 store）
├── 🧩 Canvas 拆分（ElementWrapper + LockBadge）
├── 📚 模板库扩充至 50+
├── 📱 响应式布局（768px 断点）
└── 📖 新用户引导（首次弹窗 + tooltip）

P2 (本季)
├── ☁️ 云存储 + 多端同步
├── 🤖 AI 标题生成（接入 LLM）
├── 📊 数据埋点 + 后台看板
├── 🔌 图片搜索集成
└── 🖼️ 导出格式扩充（JPG/SVG/PDF）

P3 (未来)
├── 👥 团队协作
├── 📱 手机版编辑器
├── 🚀 一键发布到平台
└── 🎨 AI 自动生成模板
```

---

## 五、总结：最该先做的 5 件事

| # | 事项 | 维度 | 预估工时 | 用户影响 |
|---|------|------|----------|----------|
| 1 | **修复抠图**（降级提示 + 自建模型通道）| 产品 + 架构 | 1d | 🔴 功能不可用 |
| 2 | **开启 strict 模式，修掉全量 as any** | 架构 | 2d | 🟡 预防线上崩溃 |
| 3 | **核心算法加测试** | 架构 | 1d | 🟡 重构安全网 |
| 4 | **Store + Canvas 拆分** | 架构 | 3d | 🟢 降低维护成本 |
| 5 | **新用户引导** | 产品 | 1d | 🔴 首次体验 |
